## 1. Rule and CSS

- [x] 1.1 Confirmar `.cursor/rules/static-chat-ui.mdc` (globs `app/static/**`): chrome compacto, no `flex-basis` de fila en layout columna
- [x] 1.2 En `app/static/index.html`, bajo `@media (max-width: 480px)`: `.toolbar span { flex: 0 0 auto; }` y apretar padding/gaps del footer y toolbar

## 2. Verification

- [x] 2.1 Revisar a ~360px: hint, “Nueva consulta” e input juntos, sin hueco grande entre el hint y el composer
